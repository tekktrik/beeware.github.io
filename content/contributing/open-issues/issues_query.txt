query {
  organization (login: "beeware") {
    repository (name: "{{ repo_name }}") {
      name,
      issues (
        first: 100,
        orderBy: {
          field: CREATED_AT,
          direction: DESC
        }
        after: {{ after_cursor }},
        filterBy: {states: OPEN}
      ) {
        nodes {
          title
          url
          labels (
            first: 25
          ) { 
            nodes {
              name
            }
          }
        }
        pageInfo {
          endCursor
          hasNextPage
        }
      }
    }
  }
}